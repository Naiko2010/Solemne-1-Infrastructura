/** Formatted role labels (after formatRoleLabel in AuthContext). Used for all role comparisons. */
export const WORKER_ROLES = ['Empleado', 'Cajero']
export const ADMIN_ROLES = ['Admin', 'Superadmin']
